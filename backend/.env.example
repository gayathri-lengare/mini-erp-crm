# Server Port
PORT=5000

# PostgreSQL Connection String
# For local cluster on port 5433: postgresql://postgres:postgres@localhost:5433/mini_erp
# For standard local Postgres (port 5432): postgresql://postgres:postgres@localhost:5432/mini_erp
# For cloud hosted (Neon/Supabase/Render): postgresql://user:password@host:5432/mini_erp?sslmode=require
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/mini_erp

# JWT Secret Key for token signing
JWT_SECRET=super_secret_jwt_key_mini_erp_wholesale_portal_2026

# Allowed Frontend URL for CORS
FRONTEND_URL=http://localhost:5173
